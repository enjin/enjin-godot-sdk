query {
    result: EnjinUsers(
        id: $id,
        name: $name,
        me: $me,
        pagination: $pagination
    ) {
        items {
            ...UserFragment
        }
        cursor {
            ...PaginationCursorFragment
        }
    }
}

$id: Int
$name: String
$me: Boolean
$pagination: PaginationInput!